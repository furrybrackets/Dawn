import { AST, TextGeneratorOptions } from "./types";

export default class TextGenerator {
    constructor(ast: AST, options: TextGeneratorOptions) {
        // ...
    }

    generate(): string {
        let text = "";
        // ...
        return text;
    };
};